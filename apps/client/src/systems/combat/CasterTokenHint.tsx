export function CasterTokenHint() {
  return (
    <p
      className="font-ui text-[10px] px-2 py-1.5 rounded"
      style={{ color: 'var(--color-text-secondary)', border: '1px dashed var(--color-border)' }}
    >
      Open a token&apos;s actions or select a caster token on the map to place this area
    </p>
  );
}
