# Production @ https://grimoire.gamer.gd/
# On Render: add VITE_CLERK_PUBLISHABLE_KEY in Environment (used at build time)
#
# Do NOT set VITE_SERVER_URL — same-origin /api + /socket.io on grimoire.gamer.gd

VITE_CLERK_PUBLISHABLE_KEY=pk_test_...
