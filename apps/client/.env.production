# Render-only (SERVE_CLIENT=1): leave both empty — uses window.location.origin.
# Split deploy: set VITE_SERVER_URL to your Render API origin.
VITE_SERVER_URL=
# Optional canonical URL for invite links only — do NOT use for OAuth (uses current browser URL).
# VITE_APP_URL=https://grimoire.co.uk
